# Palermo issue #240 scaffold requires no environment variables.
#
# Add environment variable names only in the implementation issue that owns
# the corresponding trusted boundary. Never commit real values or secrets.
